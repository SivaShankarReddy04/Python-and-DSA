rows = int(input("Enter no of rows: "))
num = 1   

for i in range(1, rows + 1):
    temp = []
    for j in range(i):
        temp.append(num)
        num += 1
    if i % 2 == 0:
        temp.reverse()
    print(' * '.join(map(str, temp)))
